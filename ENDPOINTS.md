# publish-hub
## Endpoints

### Все пользователи

**GET** /users
* Авторизован: администратор
---

### Активность пользователя

**GET** /users/{userId}
* Публичный.
---

### Создание поста с комментариями от автора

**POST** /users/{userId}/posts
* Авторизован.

```java
record PostCreateRequest(
        Long id,
        String title,
        String content,
        Long authorId,
        List<CommentDto> comments
) {
}
```

---

### Все посты автора

**GET** /users/{userId}/posts
* Публичный.
---

### Пост автора

**GET** /users/{userId}/posts/{postId}
* Публичный: комментарии только подтверждённые, иначе 401.
* Авторизован:
  * автор или администратор(модератор) - все комментарии
  * остальные - комментарии только подтверждённые, иначе 403
---

### Создание комментария от пользователя к посту

**POST** /users/{userId}/posts/{postId}/comments
* Авторизован.
---

### Комментарии от пользователя к посту

**GET** /users/{userId}/posts/{postId}/comments
* Публичный: комментарии только подтверждённые, иначе 401.
* Авторизован:
  * автор или администратор(модератор) - все комментарии,
  * остальные - комментарии только подтверждённые, иначе 403
---

### Удаление комментариев от пользователя к посту

**DELETE** /users/{userId}/posts/{postId}/comments
* Авторизован. Удалить может или автор поста, или администратор(модератор).
---

### Изменение комментария от пользователя к посту [TODO code]

**PATCH** /users/{userId}/posts/{postId}/comments/{commentsId}
* Авторизован. Изменяет автор комментария или администратор(модератор) только контента
---

### Комментарий от пользователя к посту

**GET** /users/{userId}/posts/{postId}/comments/{commentsId}
* Публичный: комментарии только подтверждённые, иначе 401.
* Авторизован:
  * автор или администратор(модератор) - все комментарии,
  * остальные - комментарии только подтверждённые, иначе 403
---

### Удаление комментария от пользователя к посту

**DELETE** /users/{userId}/posts/{postId}/comments/{commentsId}
* Авторизован: автор комментария/поста, администратор(модератор).
---

### Комментарий прошёл модерацию

**PATCH** /users/{userId}/posts/{postId}/comments/{commentsId}/approve
* Авторизован: автор поста, администратор(модератор).
---

### Комментарий не прошёл модерацию

**PATCH** /users/{userId}/posts/{postId}/comments/{commentsId}/reject
* Авторизован: автор поста, администратор(модератор).
---

### Все комментарии пользователя

**GET** /users/{userId}/comments
* Авторизован.
---

### Комментарий пользователя [TODO code]

**GET** /users/{userId}/comments/{commentsId}
* Авторизован.
---

### Последние посты с комментариями [CHANGE: URL /post/recent, access - public]

**GET** /users/{userId}/recent
* Публичный: комментарии только подтверждённые, иначе 401.
* Авторизован:
  * автор или администратор(модератор) - все комментарии,
  * остальные - комментарии только подтверждённые, иначе 403
---

### Активность пользователя

**GET** /users/{userId}/activity
* Публичный.
* Авторизован: автор, администратор(модератор).
---

### Все посты [TODO code FILTER]

**GET** /posts
* Публичный: без фильтра как `GET /post/recent`
* Авторизован: с фильтром (и кнопкой -_-)
---

### Пост

**GET** /posts/{postId}
* Публичный.
---

### Комментарии к посту

**GET** /posts/{postId}/comments
* Публичный: комментарии только подтверждённые, иначе 401.
* Авторизован:
  * автор или администратор(модератор) - все комментарии,
  * остальные - комментарии только подтверждённые, иначе 403
---

### Комментарии к посту

**GET** /posts/{postId}/comments/{commentsId}
* Публичный: комментарии только подтверждённые, иначе 401.
* Авторизован:
  * автор или администратор(модератор) - все комментарии,
  * остальные - комментарии только подтверждённые, иначе 403
---

### Посты с тегами

**GET** /posts/tags/
* Авторизован.
---

### Посты с тегом [REMOVE]

**GET** /posts/tags/{tagId}
* 
---